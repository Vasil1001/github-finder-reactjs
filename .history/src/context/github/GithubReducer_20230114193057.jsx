const githubReducer = (state, action) => {
    switch (action.type) {
        case 'GET_USERS':
            return {
                
            }
        default:
            return state
    }
}

export default githubReducer